# Copy to .env and fill in values (never commit .env)

# ZEGOCLOUD Live Streaming (Token04 — keep ServerSecret on the server only)
ZEGO_APP_ID=1495665265
ZEGO_SERVER_SECRET=
# 64-character AppSign from the ZEGO console (not ServerSecret). Required for AI beauty / Effects.
ZEGO_APP_SIGN=

# Google Gemini — shop recommendations, captions, moderation
GEMINI_API_KEY=
